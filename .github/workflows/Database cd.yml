name: Database cd

on:
 workflow_dispatch:
 
env: 
  Workflow2_PAT_TOKEN_GITHUB: ${{ secrets.Workflow2_PAT_TOKEN_GITHUB }}
  
jobs:
  caller-Database-deploy:
     runs-on: self-hosted
     steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
  trigger_database:
    uses: lf-common-repo/shared-workflow/.github/workflows/Database CD.yml@main

         
        
     
     
